# Dependencies
node_modules/
package-lock.json   # หรือ yarn.lock / pnpm-lock.yaml แล้วแต่ใช้ตัวไหน

# Build outputs (แต่ตอนนี้ห้าม ignore เพราะ deploy ด้วย dist)
# dist/            ← ต้องลบหรือ comment บรรทัดนี้ออก

# Environment files
.env
.env.local
.env.*.local

# Editor & OS files
.vscode/
.idea/
.DS_Store
*.log

# TypeScript cache
*.tsbuildinfo